@model IEnumerable<PetGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PetGroupName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PetGroupName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PetGroupID" class="btn btn-sm btn-outline-primary me-1">
                <i class="bi bi-pencil-square"></i>
                </a>
                <a asp-action="Edit" asp-route-id="@item.PetGroupID" class="btn btn-sm btn-outline-primary me-1">
                <i class="bi bi-pencil-square"></i>
                <a asp-action="Edit" asp-route-id="@item.PetGroupID" class="btn btn-sm btn-outline-primary me-1">
                <i class="bi bi-pencil-square"></i>
                    </a>
                </a>
            </td>
        </tr>
}
    </tbody>
</table>
