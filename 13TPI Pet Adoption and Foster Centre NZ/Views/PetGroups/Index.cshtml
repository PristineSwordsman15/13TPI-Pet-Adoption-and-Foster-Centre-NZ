@model IEnumerable<_13TPI_Pet_Adoption_and_Foster_Centre_NZ.Models.PetGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PetGroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PetGroupDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PetID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PetGroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PetGroupDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PetID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PetGroupID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PetGroupID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PetGroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
